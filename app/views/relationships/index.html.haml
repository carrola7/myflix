%section.people
  %header
    %h2.inline People I Follow
    = link_to 'Invite a Friend', new_invitation_path, class: 'btn btn-default ml-5 inline'
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - current_user.following.each do |user|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('knwang@gmail.com'.downcase)}?s=40")
            = link_to user.full_name, user
          %td.extra-padding 
            = user.queue_items.count
          %td.extra-padding 
            = user.followers.count
          %td.extra-padding
            = link_to relationship_path(user), method: :delete do
              %i.glyphicon.glyphicon-remove

